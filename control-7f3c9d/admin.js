let supabase;const auth=document.querySelector('#auth'),dashboard=document.querySelector('#dashboard'),message=document.querySelector('#message');const loginForm=document.querySelector('#loginForm'),registerForm=document.querySelector('#registerForm'),resetForm=document.querySelector('#resetForm'),newPasswordForm=document.querySelector('#newPasswordForm');const books=document.querySelector('#books');const $=id=>document.getElementById(id);const msg=t=>message.textContent=t||'';
(async()=>{try{const cfg=await fetch('/api/config').then(r=>r.json());if(!cfg.url||!cfg.anonKey)throw new Error('Supabase ещё не подключён.');supabase=window.supabase.createClient(cfg.url,cfg.anonKey);document.querySelectorAll('[data-eye]').forEach(b=>b.onclick=()=>{const i=$(b.dataset.eye);i.type=i.type==='password'?'text':'password';b.textContent=i.type==='password'?'◉':'◎';});$('loginTab').onclick=()=>switchForm('login');$('registerTab').onclick=()=>switchForm('register');$('forgot').onclick=()=>switchForm('reset');$('backLogin').onclick=()=>switchForm('login');loginForm.onsubmit=login;registerForm.onsubmit=register;resetForm.onsubmit=resetPassword;newPasswordForm.onsubmit=saveNewPassword;$('logout').onclick=()=>supabase.auth.signOut();supabase.auth.onAuthStateChange((event,session)=>{if(event==='PASSWORD_RECOVERY'){showAuth();switchForm('new');return}if(session)checkAdmin(session);else showAuth();});const {data:{session}}=await supabase.auth.getSession();if(session)checkAdmin(session);}catch(e){msg(e.message);}})();
function switchForm(which){loginForm.hidden=which!=='login';registerForm.hidden=which!=='register';resetForm.hidden=which!=='reset';newPasswordForm.hidden=which!=='new';$('loginTab').classList.toggle('active',which==='login');$('registerTab').classList.toggle('active',which==='register');msg('');}function showAuth(){auth.hidden=false;dashboard.hidden=true;}async function checkAdmin(session){const {data,error}=await supabase.from('admin_profiles').select('role').eq('id',session.user.id).maybeSingle();if(error||data?.role!=='admin'){await supabase.auth.signOut();showAuth();msg('Этот аккаунт не является администратором.');return}auth.hidden=true;dashboard.hidden=false;loadBooks();}async function login(e){e.preventDefault();msg('');const {error}=await supabase.auth.signInWithPassword({email:$('loginEmail').value.trim(),password:$('loginPassword').value});if(error)msg(error.message);}async function register(e){e.preventDefault();msg('');if($('registerPassword').value!==$('registerPassword2').value)return msg('Пароли не совпадают.');const {error}=await supabase.auth.signUp({email:$('registerEmail').value.trim(),password:$('registerPassword').value,options:{emailRedirectTo:location.href.split('#')[0]}});if(error)return msg(error.message);registerForm.reset();msg('Письмо для подтверждения отправлено. Подтвердите почту, затем войдите.');}async function resetPassword(e){e.preventDefault();msg('');const {error}=await supabase.auth.resetPasswordForEmail($('resetEmail').value.trim(),{redirectTo:location.href.split('#')[0]});if(error)msg(error.message);else{resetForm.reset();msg('Письмо для сброса пароля отправлено.');}}async function saveNewPassword(e){e.preventDefault();const {error}=await supabase.auth.updateUser({password:$('newPassword').value});if(error)return msg(error.message);newPasswordForm.reset();msg('Пароль изменён. Теперь войдите с новым паролем.');await supabase.auth.signOut();switchForm('login');}
async function loadBooks(){books.innerHTML='<div class="muted">Загрузка…</div>';const r=await fetch('/api/books');const data=await r.json();if(!data.books?.length){books.innerHTML='<div class="muted">Книг пока нет.</div>';return}books.innerHTML=data.books.map(b=>`<article class="book"><div><strong>${esc(b.title)}</strong><small>${esc(b.author||'Автор не указан')} · ${size(b.file_size)}</small></div><div class="actions"><a href="${esc(b.file_url)}" target="_blank" rel="noopener">Открыть</a> <button class="danger" data-url="${encodeURIComponent(b.file_url)}">Удалить</button></div></article>`).join('');books.querySelectorAll('.danger').forEach(b=>b.onclick=()=>removeBook(decodeURIComponent(b.dataset.url)));}async function removeBook(url){if(!confirm('Удалить эту книгу?'))return;const {data:{session}}=await supabase.auth.getSession();const r=await fetch('/api/delete',{method:'POST',headers:{Authorization:`Bearer ${session.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({url})});if(!r.ok)alert((await r.json()).error||'Ошибка удаления');else loadBooks();}
$('bookForm').onsubmit=async e=>{e.preventDefault();const out=$('uploadMessage'),file=$('pdf').files[0];out.textContent='';if(!file)return out.textContent='Выберите PDF.';if(file.size>200*1024*1024)return out.textContent='Файл больше 200 МБ.';if(!file.name.toLowerCase().endsWith('.pdf')||file.type!=='application/pdf')return out.textContent='Разрешены только PDF-файлы.';const head=new Uint8Array(await file.slice(0,5).arrayBuffer());if(new TextDecoder().decode(head)!=='%PDF-')return out.textContent='Файл не является настоящим PDF.';const {data:{session}}=await supabase.auth.getSession();if(!session)return out.textContent='Сессия истекла.';out.textContent='Загрузка…';try{const path=`books/${Date.now()}-${crypto.randomUUID()}.pdf`;const {data,error}=await supabase.storage.from('books').upload(path,file,{contentType:'application/pdf',upsert:false});if(error)throw error;const {data:pub}=supabase.storage.from('books').getPublicUrl(data.path);const {error:dbError}=await supabase.from('books').insert({title:$('title').value.trim(),author:$('author').value.trim(),file_path:data.path,file_url:pub.publicUrl,file_size:file.size,created_by:session.user.id});if(dbError){await supabase.storage.from('books').remove([data.path]);throw dbError}e.target.reset();out.textContent='Книга опубликована.';loadBooks();}catch(err){out.textContent=err.message||'Ошибка загрузки.'}};function esc(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function size(n){if(!n)return '';let i=0,x=n,u=['Б','КБ','МБ','ГБ'];while(x>=1024&&i<3){x/=1024;i++}return `${x.toFixed(i?1:0)} ${u[i]}`}