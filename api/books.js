export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});try{const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_ANON_KEY;if(!url||!key)return res.status(500).json({error:'Supabase is not configured',books:[]});const r=await fetch(`${url}/rest/v1/books?select=id,title,author,file_url,file_size,created_at&order=created_at.desc`,{headers:{apikey:key,Authorization:`Bearer ${key}`}});const data=await r.json();if(!r.ok)throw new Error('Library unavailable');res.setHeader('Cache-Control','s-maxage=30, stale-while-revalidate=120');return res.status(200).json({books:data});}catch{return res.status(500).json({error:'Библиотека временно недоступна.',books:[]});}}